{
  "name": "portafolio",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "serve dist --listen ${PORT:-3000} --no-clipboard",
    "check": "astro check"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.0.0",
    "serve": "^14.2.4",
    "tailwindcss": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
