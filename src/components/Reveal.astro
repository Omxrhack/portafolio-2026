---
interface Props { delay?: number; class?: string; }
const { delay = 0, class: className = '' } = Astro.props;
---
<span class:list={['mask', 'reveal', className]} style={`--d:${delay}ms`}>
  <slot />
</span>
